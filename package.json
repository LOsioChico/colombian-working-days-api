{
  "name": "colombian-working-days-api",
  "type": "module",
  "scripts": {
    "dev": "yarn types && wrangler dev",
    "deploy": "wrangler deploy --minify",
    "types": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.9.7"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
